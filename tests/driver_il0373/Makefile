include ../Makefile.tests_common

BOARD := bluepill

USEMODULE += xtimer
USEMODULE += il0373

CFLAGS += -DDISPLAY_X=212
CFLAGS += -DDISPLAY_Y=104
CFLAGS += -DIL0373_PARAM_CS=GPIO_PIN\(PORT_B,12\)
CFLAGS += -DIL0373_PARAM_DC=GPIO_PIN\(PORT_A,10\)
CFLAGS += -DIL0373_PARAM_RST=GPIO_PIN\(PORT_A,9\)
CFLAGS += -DIL0373_PARAM_BUSY=GPIO_PIN\(PORT_A,8\)

include $(RIOTBASE)/Makefile.include
